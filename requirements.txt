crawl4ai==0.8.6
# Stealth Playwright fork — drives the JSF insolvency portal
# (insolvenzbekanntmachungen.de) headless for authoritative checks. Reuses the
# Chromium crawl4ai already installs; prod deploy must run `patchright install
# chromium` if that browser isn't present.
patchright==1.59.1
pydantic==2.13.4
streamlit==1.57.0
rapidfuzz==3.14.5
httpx==0.28.1
openai==2.36.0
python-dotenv==1.2.2
# Streamlit 1.57 pulls in starlette 1.x; older FastAPI pins starlette<0.42 and
# breaks Streamlit on import. Use a recent FastAPI release that supports
# starlette 1.x (>=0.119.0 is the first such tag).
fastapi>=0.119.0
uvicorn[standard]>=0.34.0
